
export default function CheckMove(piece, srcX, srcY, destX, destY, board, pieces) {
    var possible = true

    console.log(piece, srcX, srcY, destX, destY, board, pieces)

    return (
    {possible}
)
}
